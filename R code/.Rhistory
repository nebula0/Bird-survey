#width = 60* sqrt(data$alian + data$native + data$naturlized + data$endemic) /
# sqrt(max(data$native)), transitionTime = 0
width = 45, height = 45
)
basemap %>%
addMinicharts(
data$X, data$Y,
type = "pie",
chartdata = select(data, alian, endemic),
colorPalette = colors,
#width = 60* sqrt(data$alian + data$native + data$naturlized + data$endemic) /
# sqrt(max(data$native)), transitionTime = 0
width = 45, height = 45
)
basemap %>%
addMinicharts(
data$X, data$Y,
type = "pie",
chartdata = select(data, alian, endemic),
colorPalette = colors,
#width = 60* sqrt(data$alian + data$native + data$naturlized + data$endemic) /
# sqrt(max(data$native)), transitionTime = 0
width = 40
)
basemap %>%
addMinicharts(
data$X, data$Y,
type = "pie",
chartdata = select(data, alian, endemic),
colorPalette = colors,
#width = 60* sqrt(data$alian + data$native + data$naturlized + data$endemic) /
# sqrt(max(data$native)), transitionTime = 0
width = 30
)
basemap %>%
addMinicharts(
data$X, data$Y,
type = "pie",
chartdata = select(data, alian, endemic),
colorPalette = colors,
#width = 60* sqrt(data$alian + data$native + data$naturlized + data$endemic) /
# sqrt(max(data$native)), transitionTime = 0
width = 20
)
data
colors <- c("#00b2ca", "#7dcfb6", "#fbd1a2", "#f79256")
basemap %>%
addMinicharts(
data$X, data$Y,
type = "pie",
chartdata = select(data, endemic, native, naturalized, endemic),
colorPalette = colors,
#width = 60* sqrt(data$alian + data$native + data$naturlized + data$endemic) /
# sqrt(max(data$native)), transitionTime = 0
width = 20
)
colors <- c("#0d3b66", "#7dcfb6", "#fbd1a2", "#f79256")
basemap = leaflet() %>%
addTiles() %>%
addProviderTiles("Esri.WorldGrayCanvas") %>%
setView(121.53815856394074, 25.018035617070748, zoom=15)
basemap %>%
addMinicharts(
data$X, data$Y,
type = "pie",
chartdata = select(data, endemic, native, naturalized, endemic),
colorPalette = colors,
#width = 60* sqrt(data$alian + data$native + data$naturlized + data$endemic) /
# sqrt(max(data$native)), transitionTime = 0
width = 20
)
colors <- c("#0d3b66", "#7dcfb6", "#fb5607", "#f79256")
basemap = leaflet() %>%
addTiles() %>%
addProviderTiles("Esri.WorldGrayCanvas") %>%
setView(121.53815856394074, 25.018035617070748, zoom=15)
basemap %>%
addMinicharts(
data$X, data$Y,
type = "pie",
chartdata = select(data, endemic, native, naturalized, endemic),
colorPalette = colors,
#width = 60* sqrt(data$alian + data$native + data$naturlized + data$endemic) /
# sqrt(max(data$native)), transitionTime = 0
width = 20
)
library(knitr)
library(leaflet)
library(leaflet.minicharts)
library(dplyr)
Sys.setlocale("LC_ALL", "Chinese (Traditional)_Taiwan.950")
data <-read.csv("data/bird_origin_pi_chart_data.csv")
colors <- c("#0d3b66", "#7dcfb6", "#fb5607", "#f79256")
basemap = leaflet() %>%
addTiles() %>%
addProviderTiles("Esri.WorldGrayCanvas") %>%
setView(121.53815856394074, 25.018035617070748, zoom=15)
basemap %>%
addMinicharts(
data$X, data$Y,
type = "pie",
chartdata = select(data, endemic, native, naturalized, endemic),
colorPalette = colors,
#width = 60* sqrt(data$alian + data$native + data$naturlized + data$endemic) /
# sqrt(max(data$native)), transitionTime = 0
width = 20
)
basemap %>%
addMinicharts(
data$X, data$Y,
type = "pie",
chartdata = select(data, endemic, native, naturalized, endemic),
colorPalette = colors,
#width = 60* sqrt(data$alian + data$native + data$naturlized + data$endemic) /
# sqrt(max(data$native)), transitionTime = 0
width = 30
)
basemap %>%
addMinicharts(
data$X, data$Y,
type = "pie",
chartdata = select(data, endemic, native, naturalized, alian),
colorPalette = colors,
#width = 60* sqrt(data$alian + data$native + data$naturlized + data$endemic) /
# sqrt(max(data$native)), transitionTime = 0
width = 30
)
basemap %>%
addMinicharts(
data$X, data$Y,
type = "pie",
chartdata = select(data, endemic, native, naturalized, invasive),
colorPalette = colors,
#width = 60* sqrt(data$alian + data$native + data$naturlized + data$endemic) /
# sqrt(max(data$native)), transitionTime = 0
width = 30
)
library(knitr)
library(leaflet)
library(leaflet.minicharts)
library(dplyr)
Sys.setlocale("LC_ALL", "Chinese (Traditional)_Taiwan.950")
data <-read.csv("data/bird_origin_pi_chart_data.csv")
colors <- c("#0d3b66", "#7dcfb6", "#fb5607", "#f79256")
basemap = leaflet() %>%
addTiles() %>%
addProviderTiles("Esri.WorldGrayCanvas") %>%
setView(121.53815856394074, 25.018035617070748, zoom=15)
basemap %>%
addMinicharts(
data$X, data$Y,
type = "pie",
chartdata = select(data, endemic, native, naturalized, invasive),
colorPalette = colors,
#width = 60* sqrt(data$alian + data$native + data$naturlized + data$endemic) /
# sqrt(max(data$native)), transitionTime = 0
width = 30
)
colors <- c("#0d3b66", "#7dcfb6", "#fb5607", "#f4d35e")
basemap = leaflet() %>%
addTiles() %>%
addProviderTiles("Esri.WorldGrayCanvas") %>%
setView(121.53815856394074, 25.018035617070748, zoom=15)
basemap %>%
addMinicharts(
data$X, data$Y,
type = "pie",
chartdata = select(data, endemic, native, naturalized, invasive),
colorPalette = colors,
#width = 60* sqrt(data$alian + data$native + data$naturlized + data$endemic) /
# sqrt(max(data$native)), transitionTime = 0
width = 30
)
basemap %>%
addMinicharts(
data$X, data$Y,
type = "pie",
chartdata = select(data, endemic, native, invasive, naturalized),
colorPalette = colors,
#width = 60* sqrt(data$alian + data$native + data$naturlized + data$endemic) /
# sqrt(max(data$native)), transitionTime = 0
width = 30
)
basemap %>%
addMinicharts(
data$X, data$Y,
type = "pie",
chartdata = select(data, endemic, native, invasive, naturalized),
colorPalette = colors,
#width = 60* sqrt(data$alian + data$native + data$naturlized + data$endemic) /
# sqrt(max(data$native)), transitionTime = 0
width = 30
) %>%
addLegendSize(
values = data$native,
color = 'red',
fillColor = 'red',
opacity = .5,
title = 'Depth',
shape = 'circle',
orientation = 'horizontal',
breaks = 5)
install.packages("leaflegend")
library(leaflegend)
Sys.setlocale("LC_ALL", "Chinese (Traditional)_Taiwan.950")
data <-read.csv("data/bird_origin_pi_chart_data.csv")
colors <- c("#0d3b66", "#7dcfb6", "#fb5607", "#f4d35e")
basemap = leaflet() %>%
addTiles() %>%
addProviderTiles("Esri.WorldGrayCanvas") %>%
setView(121.53815856394074, 25.018035617070748, zoom=15)
basemap %>%
addMinicharts(
data$X, data$Y,
type = "pie",
chartdata = select(data, endemic, native, invasive, naturalized),
colorPalette = colors,
#width = 60* sqrt(data$alian + data$native + data$naturlized + data$endemic) /
# sqrt(max(data$native)), transitionTime = 0
width = 30
) %>%
addLegendSize(
values = data$native,
color = 'red',
fillColor = 'red',
opacity = .5,
title = 'Depth',
shape = 'circle',
orientation = 'horizontal',
breaks = 5)
basemap %>%
addMinicharts(
data$X, data$Y,
type = "pie",
chartdata = select(data, endemic, native, invasive, naturalized),
colorPalette = colors,
#width = 60* sqrt(data$alian + data$native + data$naturlized + data$endemic) /
# sqrt(max(data$native)), transitionTime = 0
width = 30
) %>%
addLegendSize(
values = data$native,
color = 'red',
fillColor = 'red',
opacity = .5,
title = 'Depth',
shape = 'circle',
orientation = 'horizontal',
breaks = 5,
position = 'topleft')
basemap %>%
addMinicharts(
data$X, data$Y,
type = "pie",
chartdata = select(data, endemic, native, invasive, naturalized),
colorPalette = colors,
#width = 60* sqrt(data$alian + data$native + data$naturlized + data$endemic) /
# sqrt(max(data$native)), transitionTime = 0
width = 30
) %>%
addLegendSize(
values = data$native,
color = 'red',
fillColor = 'red',
opacity = .5,
title = 'Depth',
shape = 'circle',
orientation = 'horizontal',
breaks = 5,
position = 'topright')
basemap %>%
addMinicharts(
data$X, data$Y,
type = "pie",
chartdata = select(data, endemic, native, invasive, naturalized),
colorPalette = colors,
#width = 60* sqrt(data$alian + data$native + data$naturlized + data$endemic) /
# sqrt(max(data$native)), transitionTime = 0
width = 30
) %>%
addLegendSize(
values = data$native,
color = 'gray',
fillColor = 'red',
opacity = .5,
title = 'Depth',
shape = 'circle',
orientation = 'horizontal',
breaks = 5,
position = 'topright')
basemap %>%
addMinicharts(
data$X, data$Y,
type = "pie",
chartdata = select(data, endemic, native, invasive, naturalized),
colorPalette = colors,
#width = 60* sqrt(data$alian + data$native + data$naturlized + data$endemic) /
# sqrt(max(data$native)), transitionTime = 0
width = 30
) %>%
addLegendSize(
values = data$native,
color = 'gray',
fillColor = 'gray',
opacity = .5,
title = 'Depth',
shape = 'circle',
orientation = 'horizontal',
breaks = 5,
position = 'topright')
data <-read.csv("data/bird_origin_pie_shannon.csv")
data
basemap %>%
addMinicharts(
data$X, data$Y,
type = "pie",
chartdata = select(data, Endemic, Native, Invasive, Naturalized),
colorPalette = colors,
#width = 60* sqrt(data$alian + data$native + data$naturlized + data$endemic) /
# sqrt(max(data$native)), transitionTime = 0
width = data$Shannon.Diversity
) %>%
addLegendSize(
values = data$native,
color = 'gray',
fillColor = 'gray',
opacity = .5,
title = 'Depth',
shape = 'circle',
orientation = 'horizontal',
breaks = 5,
position = 'topright')
basemap %>%
addMinicharts(
data$X, data$Y,
type = "pie",
chartdata = select(data, Endemic, Native, Invasive, Naturalized),
colorPalette = colors,
#width = 60* sqrt(data$alian + data$native + data$naturlized + data$endemic) /
# sqrt(max(data$native)), transitionTime = 0
width = data$Shannon.Diversity
) %>%
addLegendSize(
values = data$Native,
color = 'gray',
fillColor = 'gray',
opacity = .5,
title = 'Depth',
shape = 'circle',
orientation = 'horizontal',
breaks = 5,
position = 'topright')
basemap %>%
addMinicharts(
data$X, data$Y,
type = "pie",
chartdata = select(data, Endemic, Native, Invasive, Naturalized),
colorPalette = colors,
#width = 60* sqrt(data$alian + data$native + data$naturlized + data$endemic) /
# sqrt(max(data$native)), transitionTime = 0
width = data$Shannon.Diversity
) %>%
addLegendSize(
values = data$Shannon.Diversity,
color = 'gray',
fillColor = 'gray',
opacity = .5,
title = 'Depth',
shape = 'circle',
orientation = 'horizontal',
breaks = 5,
position = 'topright')
basemap %>%
addMinicharts(
data$X, data$Y,
type = "pie",
chartdata = select(data, Endemic, Native, Invasive, Naturalized),
colorPalette = colors,
#width = 60* sqrt(data$alian + data$native + data$naturlized + data$endemic) /
# sqrt(max(data$native)), transitionTime = 0
width = data$Shannon.Diversity * 10
) %>%
addLegendSize(
values = data$Shannon.Diversity,
color = 'gray',
fillColor = 'gray',
opacity = .5,
title = 'Depth',
shape = 'circle',
orientation = 'horizontal',
breaks = 5,
position = 'topright')
basemap %>%
addMinicharts(
data$X, data$Y,
type = "pie",
chartdata = select(data, Endemic, Native, Invasive, Naturalized),
colorPalette = colors,
#width = 60* sqrt(data$alian + data$native + data$naturlized + data$endemic) /
# sqrt(max(data$native)), transitionTime = 0
width = data$Shannon.Diversity ^2
) %>%
addLegendSize(
values = data$Shannon.Diversity,
color = 'gray',
fillColor = 'gray',
opacity = .5,
title = 'Depth',
shape = 'circle',
orientation = 'horizontal',
breaks = 5,
position = 'topright')
basemap %>%
addMinicharts(
data$X, data$Y,
type = "pie",
chartdata = select(data, Endemic, Native, Invasive, Naturalized),
colorPalette = colors,
#width = 60* sqrt(data$alian + data$native + data$naturlized + data$endemic) /
# sqrt(max(data$native)), transitionTime = 0
width = data$Shannon.Diversity ^4
) %>%
addLegendSize(
values = data$Shannon.Diversity,
color = 'gray',
fillColor = 'gray',
opacity = .5,
title = 'Depth',
shape = 'circle',
orientation = 'horizontal',
breaks = 5,
position = 'topright')
basemap %>%
addMinicharts(
data$X, data$Y,
type = "pie",
chartdata = select(data, Endemic, Native, Invasive, Naturalized),
colorPalette = colors,
#width = 60* sqrt(data$alian + data$native + data$naturlized + data$endemic) /
# sqrt(max(data$native)), transitionTime = 0
width = data$Shannon.Diversity ^3
) %>%
addLegendSize(
values = data$Shannon.Diversity,
color = 'gray',
fillColor = 'gray',
opacity = .5,
title = 'Depth',
shape = 'circle',
orientation = 'horizontal',
breaks = 5,
position = 'topright')
basemap %>%
addMinicharts(
data$X, data$Y,
type = "pie",
chartdata = select(data, Endemic, Native, Invasive, Naturalized),
colorPalette = colors,
#width = 60* sqrt(data$alian + data$native + data$naturlized + data$endemic) /
# sqrt(max(data$native)), transitionTime = 0
width = data$Shannon.Diversity ^4
) %>%
addLegendSize(
values = data$Shannon.Diversity,
color = 'gray',
fillColor = 'gray',
opacity = .5,
title = 'Depth',
shape = 'circle',
orientation = 'horizontal',
breaks = 5,
position = 'topright')
basemap %>%
addMinicharts(
data$X, data$Y,
type = "pie",
chartdata = select(data, Endemic, Native, Invasive, Naturalized),
colorPalette = colors,
#width = 60* sqrt(data$alian + data$native + data$naturlized + data$endemic) /
# sqrt(max(data$native)), transitionTime = 0
width = data$Shannon.Diversity ^4
) %>%
addLegendSize(
values = data$Shannon.Diversity^4,
color = 'gray',
fillColor = 'gray',
opacity = .5,
title = 'Depth',
shape = 'circle',
orientation = 'horizontal',
breaks = 5,
position = 'topright')
basemap %>%
addMinicharts(
data$X, data$Y,
type = "pie",
chartdata = select(data, Endemic, Native, Invasive, Naturalized),
colorPalette = colors,
#width = 60* sqrt(data$alian + data$native + data$naturlized + data$endemic) /
# sqrt(max(data$native)), transitionTime = 0
width = data$Shannon.Diversity ^4
) %>%
addLegendSize(
values = data$Shannon.Diversity^4,
color = 'gray',
fillColor = 'gray',
opacity = .5,
title = 'Shannon Diversity ^ 4',
shape = 'circle',
orientation = 'horizontal',
breaks = 5,
position = 'topright')
